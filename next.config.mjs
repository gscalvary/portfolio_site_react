/** @type {import('next').NextConfig} */
const nextConfig = {
    // Uncomment the following lines when building for deployment as an SPA.
    // images: {
    //     unoptimized: true
    // },
    // output: 'export'
};

export default nextConfig;
